---
description: Report which tutorials are recorded vs stale or missing — no recording.
---

Invoke the **tutorial** skill in **STATUS** mode.

Follow `SKILL.md` → status: read the catalog and the specs folder and print a tight table — per
tutorial: recorded vs spec-only, `recordedAt` vs spec mtime (stale?), and any missing artifacts
(no MP4 / no screenshots / no catalog entry). **Do not record anything** in this mode.
