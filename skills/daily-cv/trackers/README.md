# daily-cv Trackers

This directory can hold local tracker files for users who choose `local` or `both` tracker mode.

- `applications.template.csv` is the starter tracker schema.
- Copy it to `applications.csv` for private local use.

Private tracker files are ignored by Git by default.